# Features

Vertical slices — one folder per business capability. Each slice owns its controller(s), service class, DTOs, and validators.
Features depend on Domain interfaces only, never on Infrastructure implementations.

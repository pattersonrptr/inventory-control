# Domain

Core business entities and repository interfaces. No dependency on EF Core, ASP.NET, or any infrastructure concern.
Each subfolder is an aggregate root with its entities and the repository interface it owns.
